	<% @assignment.contributions.each do | contribution | %> 
			<%= contribution.repo_fork %>
			<%=	contribution.user_id %>
			<%=	contribution.url %>
	    <%=	contribution.github_id %> 
			<% end %> 

